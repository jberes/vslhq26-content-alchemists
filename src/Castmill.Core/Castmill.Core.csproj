<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>


  <ItemGroup>
    <!-- Pure helpers (payload shape handling, name formatting) are unit-tested directly
         rather than only through the endpoints that use them. -->
    <InternalsVisibleTo Include="Castmill.Api.Tests" />
  </ItemGroup>

</Project>
