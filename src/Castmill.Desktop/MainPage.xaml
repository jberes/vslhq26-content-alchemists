<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ui="clr-namespace:Castmill.UI;assembly=Castmill.UI"
             x:Class="Castmill.Desktop.MainPage">

    <!-- The root component comes from the RCL, exactly as it does on web (ADR-F01).
         This shell owns no Razor of its own. -->
    <BlazorWebView x:Name="blazorWebView" HostPage="wwwroot/index.html">
        <BlazorWebView.RootComponents>
            <RootComponent Selector="#app" ComponentType="{x:Type ui:App}" />
        </BlazorWebView.RootComponents>
    </BlazorWebView>

</ContentPage>
